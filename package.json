{
  "name": "hero-clash",
  "private": true,
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev --concurrency=13",
    "lint": "turbo run lint",
    "format": "prettier --write \"**/*.{ts,tsx,md}\"",
    "hathora:start": "npm run start --workspace @hc/game-server"
  },
  "devDependencies": {
    "eslint": "^8.48.0",
    "prettier": "^3.0.3",
    "@hc/tsconfig": "*",
    "turbo": "latest"
  },
  "workspaces": {
    "packages": [
      "libs/*",
      "apps/*",
      "configs/*"
    ]
  }
}
