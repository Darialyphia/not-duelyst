{
  "name": "hero-clash",
  "private": true,
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "lint": "turbo run lint",
    "format": "prettier --write \"**/*.{ts,tsx,md}\"",
    "hathora:start": "yarn workspace @hc/game-server start"
  },
  "devDependencies": {
    "eslint": "^8.48.0",
    "prettier": "^3.0.3",
    "@hc/tsconfig": "workspace:*",
    "turbo": "latest"
  },
  "workspaces": {
    "packages": [
      "libs/*",
      "apps/*",
      "configs/*"
    ]
  }
}
